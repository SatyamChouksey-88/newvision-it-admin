export const DEFAULT_ONBOARD_ITEMS = [
  'Issue kit (laptop + charger + mouse)',
  'Create login',
  'VPN / MFA',
  'ID badge',
  'Signed handover',
];

export const DEFAULT_OFFBOARD_ITEMS = ['Recover assets', 'Disable login', 'Revoke access'];

/** India-typical first confirmation window. */
export const DEFAULT_PROBATION_DAYS = 90;
